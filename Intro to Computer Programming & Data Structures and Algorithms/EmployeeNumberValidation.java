Public class EmployeeNumberValidation {
   Public static void main(String[] args) {
      string input;
      input=JOptionPane.showInputDialog("Enter a empid lllnnnn format");
      if(isValid(input)) {
         JOptionPane. showMessage(null, "Its a valid emp id");
      } else {
         JOptionPane.showMessage(null, "Its a valid emp id");
      }